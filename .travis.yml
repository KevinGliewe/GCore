language: csharp
mono: latest
git:
  depth: false
install:
  - curl -sSL https://dot.net/v1/dotnet-install.sh | bash /dev/stdin -Version '2.1.20'
  - curl -sSL https://dot.net/v1/dotnet-install.sh | bash /dev/stdin -Version '3.1.6'
script:
  - chmod +x ./build.sh 
  - ./build.sh Publish -ApiKey $NUGET_API_KEY